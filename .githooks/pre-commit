#!/bin/sh
# Gate staged files on biome (format + lint errors). Warnings pass.
bun x biome check --staged --no-errors-on-unmatched || {
  echo "biome check failed — fix (bun run check:fix) or inspect (bun run check) before committing." >&2
  exit 1
}
